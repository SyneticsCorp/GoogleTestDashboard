# GoogleTest 결과 분석 웹 애플리케이션 기능 요구사항

## 1. 문서 정보

- 문서 상태: 구현용 초안
- 구현 언어: Python
- 대상 데이터: `GoogleTestResults/01`~`GoogleTestResults/10`의 GoogleTest XML
- 데이터 성격: Jenkins/GoogleTest 결과 분석 실습을 위한 합성 데이터
- 주요 사용자: SW 개발자, 테스트 엔지니어, Jenkins 빌드 결과 검토자

## 2. 목적

본 시스템은 Jenkins 빌드별 GoogleTest XML을 읽어 최신 테스트 결과, 빌드 트렌드, 모듈별 실패 분포 및 개별 테스트 상세 결과를 웹 화면에서 조회할 수 있도록 한다.

시스템은 다음 사용자 질문에 답할 수 있어야 한다.

1. 마지막 빌드의 테스트 결과는 어떠한가?
2. 최근 빌드의 실패율은 증가하는가, 감소하는가?
3. 실패가 주로 어느 모듈에서 발생하는가?
4. 특정 테스트, 함수, 모듈 또는 실패 메시지를 찾을 수 있는가?
5. 실패한 테스트만 모아서 원인 정보를 확인할 수 있는가?

## 3. 범위

### 3.1 포함 범위

- 파일 시스템의 GoogleTest XML 검색 및 파싱
- 빌드, 모듈, 테스트 스위트, 테스트 케이스 단위 집계
- 최신 빌드 중심의 메인 대시보드
- 빌드 및 모듈별 실패 트렌드 차트
- 빌드, 모듈 및 테스트 상세 페이지
- 텍스트 검색, 상태 필터, 실패 전용 필터, 정렬 및 페이지 이동
- XML 오류 및 집계 불일치 표시

### 3.2 제외 범위

- Jenkins 서버 API 직접 연동
- Jenkins 작업 실행 또는 재실행
- 원본 XML 수정
- 사용자 인증 및 권한 관리
- 실제 차량, ECU, 안전등급 또는 인증 판정

## 4. 입력 데이터 구조

### 4.1 빌드 디렉터리

- 기본 결과 경로는 `GoogleTestResults`이다.
- 하위 폴더 `01`~`10`은 Jenkins 빌드 번호를 나타낸다.
- 각 빌드 폴더에는 모듈별 GoogleTest XML이 존재한다.

### 4.2 XML 구조

시스템은 다음 계층을 읽어야 한다.

```text
testsuites
└─ testsuite
   ├─ properties
   └─ testcase
      └─ failure (실패한 경우)
```

### 4.3 정규화 테스트 레코드

각 `<testcase>`는 최소한 다음 필드로 정규화해야 한다.

| 필드 | 설명 | XML 근거 |
|---|---|---|
| build_id | Jenkins 빌드 식별자 | `jenkins_build_number` 또는 폴더명 |
| build_timestamp | 빌드 시각 | `testsuites@timestamp` 또는 `testsuite@timestamp` |
| module | 모듈명 | `property[name=module]` |
| suite | 테스트 스위트/Fixture | `testsuite@name` |
| function | 시험 대상 함수 | `property[name=tested_function]` |
| test_name | 테스트 케이스명 | `testcase@name` |
| classname | GoogleTest 클래스명 | `testcase@classname` |
| status | 정규화 상태 | PASSED, FAILED, ERROR, SKIPPED, DISABLED |
| duration_seconds | 수행 시간 | `testcase@time` |
| timestamp | 테스트 시각 | `testcase@timestamp` |
| test_file | 테스트 소스 파일 | `testcase@file` |
| line | 테스트 소스 줄 번호 | `testcase@line` |
| failure_type | 실패 유형 | `failure@type` |
| failure_summary | 실패 요약 | `failure@message` |
| failure_detail | 실패 상세 메시지 | `<failure>` 본문 |
| source_file | 시험 대상 소스 파일 | `property[name=source_file]` |
| synthetic_data | 합성 데이터 여부 | `property[name=synthetic_data]` |

## 5. 페이지 구성

| 페이지 | 목적 | 권장 경로 |
|---|---|---|
| 메인 대시보드 | 최신 빌드와 전체 트렌드 표시 | `/` |
| 빌드 상세 | 선택한 빌드의 전체 테스트 결과 조회 | `/builds/{build_id}` |
| 모듈 상세 | 선택한 빌드와 모듈의 결과 조회 | `/builds/{build_id}/modules/{module}` |
| 테스트 상세 | 한 테스트의 입력 속성 및 실패 정보 조회 | `/builds/{build_id}/tests/{test_id}` |
| 검색 결과 | 검색 조건에 맞는 테스트 목록 조회 | `/search` |

경로 형식은 구현 프레임워크에 맞게 변경할 수 있으나 페이지 역할과 이동 관계는 유지해야 한다.

## 6. 기능 요구사항

### 6.1 결과 수집 및 정규화

#### FR-001 결과 경로 설정

- 시스템은 GoogleTest 결과 루트 경로를 설정으로 입력받아야 한다.
- 설정이 없으면 작업 디렉터리의 `GoogleTestResults`를 기본값으로 사용해야 한다.
- 수용 기준: 유효한 경로를 지정하면 해당 경로만 검색하고, 경로가 없으면 사용자에게 오류 원인과 입력 경로를 표시한다.

#### FR-002 빌드 자동 검색

- 시스템은 결과 루트의 하위 폴더를 검색하여 빌드 목록을 생성해야 한다.
- 숫자로 변환 가능한 폴더명은 숫자 기준으로 정렬해야 한다. 예: `09` 다음은 `10`이다.
- 수용 기준: 현재 데이터에서 `01`~`10`의 10개 빌드를 순서대로 식별한다.

#### FR-003 XML 자동 검색

- 시스템은 각 빌드 폴더에서 `.xml` 파일을 재귀적으로 검색해야 한다.
- 동일 파일을 두 번 읽지 않아야 한다.
- 수용 기준: 현재 데이터에서 빌드별 10개, 전체 100개 XML을 식별한다.

#### FR-004 GoogleTest XML 파싱

- 시스템은 `testsuites`, `testsuite`, `properties`, `testcase`, `failure` 요소와 관련 속성을 파싱해야 한다.
- `<failure>`가 없는 테스트도 정상 레코드로 생성해야 한다.
- 수용 기준: 현재 데이터에서 빌드별 1,200개, 전체 12,000개 테스트 레코드를 생성한다.

#### FR-005 모듈 및 함수 식별

- 시스템은 `property[name=module]` 값을 모듈명으로 사용해야 한다.
- 모듈 속성이 없으면 `testcase@classname`의 첫 구간, 그마저 없으면 XML 파일명을 대체값으로 사용해야 한다.
- 시스템은 `property[name=tested_function]` 값을 함수명으로 사용하고, 값이 없으면 테스트 이름에서 함수 후보를 표시해야 한다.
- 수용 기준: 현재 데이터의 10개 모듈과 모듈별 6개 함수/스위트를 식별한다.

#### FR-006 테스트 상태 판정

- 시스템은 `<failure>`가 있으면 `FAILED`, `<error>`가 있으면 `ERROR`, `<skipped>`가 있으면 `SKIPPED`, 비실행 표시가 있으면 `DISABLED`, 그 외에는 `PASSED`로 분류해야 한다.
- 하나의 테스트에 여러 상태 근거가 있으면 `ERROR > FAILED > SKIPPED > DISABLED > PASSED` 순서로 판정해야 한다.
- 수용 기준: 실패 레코드 수와 XML의 실제 `<failure>` 개수가 일치한다.

#### FR-007 집계값 계산

- 시스템은 XML 루트의 선언값만 사용하지 않고 정규화한 테스트 레코드로 집계값을 다시 계산해야 한다.
- 다음 계산식을 사용해야 한다.
  - 전체 테스트 수 = 정규화 테스트 레코드 수
  - 실패 수 = `FAILED` 레코드 수
  - 오류 수 = `ERROR` 레코드 수
  - 통과 수 = `PASSED` 레코드 수
  - 실패율 = `실패 수 / 전체 테스트 수 × 100`
- 전체 테스트 수가 0이면 실패율은 숫자 0이 아니라 `N/A`로 표시해야 한다.
- 수용 기준: 현재 데이터의 빌드별 실패율이 `8, 4, 9, 6, 3, 7, 5, 10, 4, 2%`로 계산된다.

#### FR-008 XML 선언값 대조

- 시스템은 `tests`, `failures`, `errors`, `disabled`, `skipped` 선언값과 실제 하위 노드 집계값을 대조해야 한다.
- 불일치가 있더라도 파싱 가능한 테스트는 표시하되 해당 파일과 빌드에 경고를 표시해야 한다.
- 수용 기준: 경고에는 XML 경로, 선언값, 실제 계산값이 포함된다.

### 6.2 메인 대시보드

#### FR-009 최신 빌드 판정

- 시스템은 `jenkins_build_number`의 최댓값을 최신 빌드로 판정해야 한다.
- 해당 값이 없으면 숫자 폴더명, 그다음 타임스탬프 순으로 판정해야 한다.
- 수용 기준: 현재 데이터에서 빌드 `10`을 최신 빌드로 선택한다.

#### FR-010 최신 결과 요약

- 메인 페이지 상단에는 최신 빌드 번호, 빌드 시각, 전체 테스트 수, 통과 수, 실패 수, 오류 수, 건너뜀 수, 실패율 및 총 수행 시간을 표시해야 한다.
- 실패율은 소수점 첫째 자리까지 표시해야 한다.
- 수용 기준: 빌드 `10`에 대해 전체 1,200건, 통과 1,176건, 실패 24건, 실패율 2.0%를 표시한다.

#### FR-011 이전 빌드 대비 변화

- 시스템은 최신 빌드와 직전 빌드의 실패 수 및 실패율 차이를 계산해야 한다.
- 증가, 감소, 동일 상태와 변화량을 함께 표시해야 한다.
- 수용 기준: 빌드 `10`은 빌드 `09` 대비 실패율이 4.0%에서 2.0%로 2.0%p 감소했다고 표시한다.

#### FR-012 빌드 실패율 트렌드

- 메인 페이지는 빌드 번호를 X축, 실패율을 Y축으로 하는 트렌드 차트를 표시해야 한다.
- 최신 빌드 데이터 포인트를 다른 포인트보다 명확히 강조해야 한다.
- 수용 기준: 빌드 `01`~`10`이 숫자 순서로 표시되고 실패율이 `8→4→9→6→3→7→5→10→4→2%`로 나타난다.

#### FR-013 트렌드 상세 정보 및 이동

- 트렌드 데이터 포인트에 빌드 번호, 시각, 전체 테스트 수, 실패 수 및 실패율을 표시해야 한다.
- 사용자가 데이터 포인트를 선택하면 해당 빌드 상세 페이지로 이동해야 한다.
- 수용 기준: 빌드 `08` 포인트에서 전체 1,200건, 실패 120건, 실패율 10.0%를 확인하고 빌드 `08` 상세로 이동할 수 있다.

#### FR-014 모듈별 실패 차트

- 메인 페이지는 최신 빌드의 모듈별 실패 수를 가로 막대 차트로 표시해야 한다.
- 실패 수가 많은 모듈부터 내림차순으로 정렬해야 한다.
- 각 모듈에 실패 수와 모듈 실패율을 함께 제공해야 한다.
- 기본 범위는 최신 빌드로 하고, 사용자가 특정 빌드 또는 전체 빌드 누적 범위로 전환할 수 있어야 한다.
- 수용 기준: 빌드 `10` 범위에서는 10개 모듈의 실패 수 합계가 24가 되고, 전체 빌드 누적 범위에서는 합계가 696이 된다.

#### FR-015 모듈 차트 드릴다운

- 사용자가 모듈 막대를 선택하면 최신 빌드의 해당 모듈 상세 페이지로 이동해야 한다.
- 이동한 페이지는 실패 테스트만 보기 필터를 기본 활성화해야 한다.
- 수용 기준: `ChildLockController` 막대를 선택하면 빌드 `10`, 해당 모듈, 실패 전용 조건으로 3개 실패 테스트를 표시한다.

#### FR-016 최신 실패 테스트 목록

- 메인 페이지는 최신 빌드의 실패 테스트 목록을 표시해야 한다.
- 목록에는 모듈, 함수/스위트, 테스트명, 실패 요약, 테스트 파일과 줄 번호, 수행 시간을 표시해야 한다.
- 전체 실패 보기 동작을 제공해야 한다.
- 수용 기준: 현재 데이터에서 최대 24개 실패 테스트를 확인할 수 있고 각 행에서 테스트 상세로 이동할 수 있다.

#### FR-017 빌드 이력 목록

- 메인 페이지는 전체 빌드의 번호, 시각, 테스트 수, 실패 수, 오류 수, 실패율 및 수행 시간을 표로 표시해야 한다.
- 기본 정렬은 최신 빌드 우선이어야 한다.
- 수용 기준: 현재 데이터에서 10개 빌드 행을 표시한다.

### 6.3 빌드 및 모듈 상세

#### FR-018 빌드 상세 요약

- 빌드 상세 페이지는 선택한 빌드의 요약 지표와 모듈별 집계를 표시해야 한다.
- 사용자가 빌드 번호를 이전/다음 빌드로 전환할 수 있어야 한다.
- 수용 기준: 첫 빌드에서는 이전 이동이, 마지막 빌드에서는 다음 이동이 비활성화된다.

#### FR-019 빌드 전체 테스트 목록

- 빌드 상세 페이지는 선택한 빌드의 모든 테스트를 표로 표시해야 한다.
- 표에는 상태, 모듈, 함수, 스위트, 테스트명, 수행 시간, 파일 및 줄 번호를 포함해야 한다.
- 수용 기준: 빌드 `10`에서 필터가 없으면 1,200개 레코드를 조회할 수 있다.

#### FR-020 모듈 상세 요약

- 모듈 상세 페이지는 선택한 빌드와 모듈의 전체, 통과, 실패, 오류, 건너뜀, 실패율 및 수행 시간을 표시해야 한다.
- 같은 모듈의 빌드별 실패율 추이를 표시해야 한다.
- 수용 기준: 빌드 `10`의 각 모듈은 전체 120개 테스트를 표시한다.

#### FR-021 모듈 테스트 목록

- 모듈 상세 페이지는 해당 모듈의 테스트만 표로 표시해야 한다.
- 함수 또는 테스트 스위트별로 필터링할 수 있어야 한다.
- 수용 기준: 빌드 `10`의 `ChildLockController`에서 필터가 없으면 120개 테스트를 표시한다.

### 6.4 테스트 상세 및 실패 확인

#### FR-022 테스트 상세 조회

- 사용자가 테스트 목록의 한 행을 선택하면 테스트 상세 페이지를 표시해야 한다.
- 상세 페이지는 빌드, 모듈, 함수, 스위트, 테스트명, 상태, 수행 시간, 시각, 테스트 파일, 줄 번호 및 대상 소스 파일을 표시해야 한다.
- 수용 기준: 목록에서 선택한 레코드와 상세 페이지의 식별 정보가 일치한다.

#### FR-023 실패 상세 조회

- 실패 테스트는 실패 유형, 실패 요약 및 `<failure>` 본문의 전체 내용을 보존하여 표시해야 한다.
- 줄바꿈과 들여쓰기를 유지한 코드 블록 형태로 표시해야 한다.
- 수용 기준: `Expected`와 `Actual`, 테스트 파일 경로 및 줄 번호가 잘리지 않고 표시된다.

#### FR-024 통과 테스트 상세

- 통과 테스트도 동일한 상세 페이지를 제공해야 한다.
- 실패 정보가 없을 때 빈 실패 영역을 표시하지 말고 `실패 정보 없음` 상태를 표시해야 한다.
- 수용 기준: `<failure>`가 없는 테스트 상세에서 오류가 발생하지 않는다.

#### FR-025 실패 테스트만 보기

- 빌드 상세, 모듈 상세 및 검색 결과 페이지는 `실패 테스트만` 토글을 제공해야 한다.
- 토글을 활성화하면 `FAILED` 상태만 표시해야 한다.
- 수용 기준: 빌드 `10`에서 토글을 활성화하면 정확히 24개 테스트를 표시한다.

### 6.5 검색, 필터 및 목록 조작

#### FR-026 공통 검색창

- 메인 페이지와 모든 상세 페이지의 공통 영역에 검색창을 제공해야 한다.
- 검색 대상은 모듈명, 함수명, 스위트명, 테스트명, 클래스명, 테스트 파일, 대상 소스 파일, 실패 요약 및 실패 상세이다.
- 대소문자를 구분하지 않는 부분 일치 검색을 지원해야 한다.
- 수용 기준: `EvaluateLockRequest`를 검색하면 해당 함수와 테스트명의 결과가 반환된다.

#### FR-027 현재 문맥 검색

- 빌드 상세에서 검색하면 기본 검색 범위는 현재 빌드여야 한다.
- 모듈 상세에서 검색하면 기본 검색 범위는 현재 빌드와 모듈이어야 한다.
- 테스트 상세의 공통 검색창은 현재 빌드를 기본 범위로 검색 결과 페이지를 열어야 한다.
- 수용 기준: 모듈 상세에서 다른 모듈의 결과가 섞이지 않는다.

#### FR-028 상태 및 분류 필터

- 시스템은 상태, 빌드, 모듈, 함수/스위트 필터를 제공해야 한다.
- 사용 가능한 필터 값은 현재 검색 결과에 존재하는 값으로 생성해야 한다.
- 수용 기준: 존재하지 않는 고정 모듈이나 스위트 값이 필터에 나타나지 않는다.

#### FR-029 검색과 필터 조합

- 텍스트 검색, 실패 전용 토글 및 분류 필터는 동시에 적용할 수 있어야 한다.
- 전체 초기화 동작은 모든 검색어와 필터를 제거해야 한다.
- 수용 기준: 빌드 `10` + `ChildLockController` + 실패 전용 조건에서 3개 결과를 표시하고, 초기화 후 1,200개로 돌아간다.

#### FR-030 정렬

- 테스트 목록은 상태, 모듈, 함수/스위트, 테스트명 및 수행 시간으로 정렬할 수 있어야 한다.
- 실패 테스트만 보기 상태에서는 기본적으로 모듈명, 함수/스위트, 테스트명 순으로 안정 정렬해야 한다.
- 수용 기준: 동일 조건에서 새로고침 후에도 목록 순서가 임의로 변하지 않는다.

#### FR-031 페이지 이동

- 테스트 목록은 대량 결과를 한 번에 모두 렌더링하지 않고 페이지 단위로 표시해야 한다.
- 페이지 크기는 최소 25, 50, 100개 선택지를 제공하고 기본값은 50개로 해야 한다.
- 현재 표시 범위와 전체 결과 수를 표시해야 한다.
- 수용 기준: 1,200개 결과에서 기본 설정 시 24페이지를 이동할 수 있다.

### 6.6 이동, 새로고침 및 오류 처리

#### FR-032 페이지 간 이동

- 메인 → 빌드 상세 → 모듈 상세 → 테스트 상세의 이동 경로를 제공해야 한다.
- 모든 상세 페이지는 상위 페이지로 돌아갈 수 있는 이동 경로를 제공해야 한다.
- 수용 기준: 브라우저 뒤로 가기 없이도 상위 화면으로 이동할 수 있다.

#### FR-033 조회 상태 유지

- 상세 페이지에서 목록으로 돌아올 때 검색어, 필터, 정렬, 페이지 번호를 유지해야 한다.
- 수용 기준: 실패 테스트 2페이지에서 상세를 본 뒤 돌아오면 같은 목록 상태가 복원된다.

#### FR-034 결과 새로고침

- 시스템은 사용자가 XML 결과를 다시 검색하고 집계를 갱신하는 새로고침 동작을 제공해야 한다.
- 새 빌드가 추가되면 새로고침 후 빌드 목록과 최신 빌드 판정에 반영해야 한다.
- 수용 기준: 기존 빌드를 재시작하지 않고도 추가된 숫자 폴더를 인식한다.

#### FR-035 손상 XML 처리

- 한 XML이 손상되어도 다른 정상 XML의 결과는 표시해야 한다.
- 손상 파일은 제외 목록에 넣고 파일 경로와 파싱 오류를 표시해야 한다.
- 수용 기준: 손상 XML 1개가 있어도 애플리케이션 전체가 종료되지 않는다.

#### FR-036 빈 결과 처리

- 빌드, 모듈, 검색 또는 필터 결과가 없으면 빈 표 대신 원인과 다음 행동을 안내해야 한다.
- 실패가 없는 빌드는 `실패 테스트 없음`으로 표시하고 모듈 차트는 0 상태를 표시해야 한다.
- 수용 기준: 결과 0건 상태에서 예외나 빈 차트 오류가 발생하지 않는다.

## 7. 현재 데이터 기반 인수 기준

아래 값은 제공된 합성 데이터가 변경되지 않았을 때의 인수 기준이다.

| 검증 항목 | 기대값 |
|---|---:|
| 빌드 수 | 10 |
| XML 파일 수 | 100 |
| 모듈 수 | 10 |
| 빌드별 테스트 수 | 1,200 |
| 전체 테스트 레코드 | 12,000 |
| 최신 빌드 | 10 |
| 최신 빌드 통과 수 | 1,176 |
| 최신 빌드 실패 수 | 24 |
| 최신 빌드 실패율 | 2.0% |
| 빌드별 실패율 | 8%, 4%, 9%, 6%, 3%, 7%, 5%, 10%, 4%, 2% |

### 7.1 최신 빌드 모듈별 인수 기준

| 모듈 | 전체 테스트 | 실패 |
|---|---:|---:|
| ChildLockController | 120 | 3 |
| CommunicationGateway | 120 | 3 |
| DiagnosticManager | 120 | 2 |
| DoorStateManager | 120 | 3 |
| LockActuator | 120 | 2 |
| PersistenceManager | 120 | 2 |
| SpeedInterlock | 120 | 3 |
| StateConsistencyMonitor | 120 | 2 |
| SwitchInput | 120 | 2 |
| VehicleSignalAdapter | 120 | 2 |
| 합계 | 1,200 | 24 |

## 8. 구현 완료 판정

다음 조건을 모두 만족하면 본 기능 요구사항의 구현이 완료된 것으로 판정한다.

1. FR-001~FR-036의 수용 기준을 통과한다.
2. 제공된 XML 100개를 파싱하고 12,000개 테스트를 중복 없이 조회한다.
3. 메인 페이지가 빌드 `10`을 최신 결과로 표시한다.
4. 실패율 트렌드와 최신 빌드 모듈별 실패 차트의 집계값이 상세 목록과 일치한다.
5. 빌드 `10`의 실패 전용 필터가 24개 결과를 표시한다.
6. 검색 결과에서 한 테스트를 선택해 실패 메시지, 파일 및 줄 번호까지 확인할 수 있다.
7. 손상 XML, 결과 없음, 실패 없음 조건에서 애플리케이션이 중단되지 않는다.
